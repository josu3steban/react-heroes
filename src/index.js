import React from 'react';
import ReactDOM from 'react-dom';
import { HeroeApp } from './HeoreApp';

ReactDOM.render(
  <React.StrictMode>
    <HeroeApp />
  </React.StrictMode>,
  document.getElementById('root')
);

